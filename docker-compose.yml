version: '3.8'

services:
  orientation-pro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: orientation-pro
    ports:
      - "5904:5904"
    restart: unless-stopped
    environment:
      - NODE_ENV=production


